{
  "name": "badge-designer-extensions",
  "version": "1.0.0",
  "description": "Shopify theme extensions for Badge Designer app",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate",
    "info": "shopify app info"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0",
    "@shopify/theme": "^3.0.0"
  }
} 